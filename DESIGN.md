## Candidate Classes
    - Customer (Name, Pet, Taskset, Schedule) 
    - Pet(Name,Specie,Breed)
    - Task(Description,Duration,Priority)
    - Schedule(Takes in tasks, sorts them and is what the user works on) -include methods(add_task,remove_task,build_schedule)

## Client Feature Request
We need to build the backend logic for the Pawpal app. The system needs to manage our customers, tracking their names and their pet needs.